pour cloner un répertoire on utilise:
git clone <URL>

pour push on utilise les trois commandes dans le bon répertoires :
git add .
git commit -m "message"
git push
git pull

on créer une branche avec:
git branch "nom de la branche"
pour switch de branche on utilise:
git switch "nom_branch_existante"
